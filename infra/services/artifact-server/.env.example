# Artifact server binding (host-ip:host-port)
ARTIFACT_BIND=0.0.0.0:8080

# Shared host directory that receives build artifacts from the runner stack.
# Must match infra/runners/github/yocto/.env when running both stacks.
ARTIFACT_ROOT=/srv/yocto-artifacts
